# data/

Local working directory for downloaded datasets, uploads, thumbnails, and
quarantined files. Everything here except this README is ignored by Git.

Datasets are reproduced from a pinned, versioned manifest (built in the data
pipeline stage), not committed.
